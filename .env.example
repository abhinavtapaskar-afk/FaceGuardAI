# ═══════════════════════════════════════════════════════════
# FACEGUARD AI - ENVIRONMENT VARIABLES TEMPLATE
# ═══════════════════════════════════════════════════════════
# 
# IMPORTANT: 
# 1. Copy this file to .env
# 2. Fill in your actual values
# 3. NEVER commit .env to version control
# 4. Rotate credentials immediately if exposed
#
# ═══════════════════════════════════════════════════════════

# Server Configuration
NODE_ENV=development
PORT=3000

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Database (Supabase)
SUPABASE_URL=your_supabase_project_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# JWT Secret (Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=your_super_secret_jwt_key_here_minimum_32_characters
JWT_REFRESH_SECRET=your_refresh_token_secret_here_minimum_32_characters
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# OpenAI API
OPENAI_API_KEY=your_openai_api_key_here

# Razorpay (Payment Gateway)
RAZORPAY_KEY_ID=your_razorpay_key_id_here
RAZORPAY_KEY_SECRET=your_razorpay_key_secret_here
RAZORPAY_ENABLED=false

# Affiliate Links
AFFILIATE_TAG=your_amazon_affiliate_tag_here
FLIPKART_ID=your_flipkart_affiliate_id_here
AFFILIATE_ENABLED=false

# Email Service (SendGrid/Mailgun)
EMAIL_SERVICE=sendgrid
EMAIL_API_KEY=your_email_service_api_key_here
EMAIL_FROM=noreply@faceguardai.com
EMAIL_FROM_NAME=FaceGuard AI

# Feature Flags
PDF_REPORTS_ENABLED=true
LEADERBOARD_ENABLED=false
SHARE_CARDS_ENABLED=false

# Security
BCRYPT_ROUNDS=10
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION=900000

# File Upload
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/jpg,image/png,image/webp

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
LOG_FILE_MAX_SIZE=5242880
LOG_FILE_MAX_FILES=5

# Monitoring (Optional)
SENTRY_DSN=your_sentry_dsn_here
SENTRY_ENABLED=false

# Redis (Optional - for session management)
REDIS_URL=redis://localhost:6379
REDIS_ENABLED=false

# ═══════════════════════════════════════════════════════════
# SECURITY NOTES:
# ═══════════════════════════════════════════════════════════
#
# 1. JWT_SECRET: Must be at least 32 characters, use crypto.randomBytes
# 2. SUPABASE_ANON_KEY: Get from Supabase project settings
# 3. OPENAI_API_KEY: Starts with 'sk-', keep it secret
# 4. RAZORPAY_KEY_SECRET: Never expose in frontend
# 5. EMAIL_API_KEY: Rotate if compromised
#
# ═══════════════════════════════════════════════════════════
